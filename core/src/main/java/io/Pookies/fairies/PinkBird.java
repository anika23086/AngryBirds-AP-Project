package io.Pookies.fairies;

// RedBird Class (Subclass of Bird)
public class PinkBird extends Bird {
    public PinkBird(float x, float y) {
        super("pinkbird.png", x, y); // Load a specific texture for RedBird
    }
}
